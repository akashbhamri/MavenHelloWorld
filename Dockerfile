We’ve requested the DB team to provide active support and terminate the job immediately if any blocking occurs. Our team will also remain available to restart the service in case the UI fails to load.